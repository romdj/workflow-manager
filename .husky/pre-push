echo "🚀 Running pre-push validation (mirrors CI pipeline)..."
echo ""

# 1. Format check
echo "📝 [1/4] Checking code formatting..."
pnpm format:check || {
  echo "❌ Format check failed. Run 'pnpm format' to fix."
  exit 1
}
echo "✅ Format check passed"
echo ""

# 2. Lint
echo "🔍 [2/4] Running linters..."
pnpm lint || {
  echo "❌ Lint failed. Run 'pnpm lint:fix' to auto-fix issues."
  exit 1
}
echo "✅ Lint passed"
echo ""

# 3. Type check (skip for now - no source files yet)
echo "🔧 [3/4] Type checking..."
echo "⏭️  Skipping type check (no source files yet)"
# pnpm type-check || {
#   echo "❌ Type check failed. Fix TypeScript errors before pushing."
#   exit 1
# }
echo "✅ Type check passed (skipped)"
echo ""

# 4. Tests (skip for now - no tests yet)
echo "🧪 [4/4] Running tests..."
echo "⏭️  Skipping tests (no test files yet)"
# pnpm test || {
#   echo "❌ Tests failed. Fix failing tests before pushing."
#   exit 1
# }
echo "✅ Tests passed (skipped)"
echo ""

echo "✨ All pre-push checks passed! Safe to push."
